import { Icons } from '@/components/ui/icons'
import { DISCORD_INVITE_URL, GITHUB_URL, GOVERNANCE_URL, SUBSTACK_URL, TWITTER_URL } from '@/src/constants'

export const siteConfig = {
  name: 'LexDAO',
  description:
    'LexDAO is a decentralized autonomous organization (DAO) that provides legal services to the Ethereum community.',
  links: {
    substack: SUBSTACK_URL,
    twitter: TWITTER_URL,
    github: GITHUB_URL,
    discord: DISCORD_INVITE_URL,
    governance: GOVERNANCE_URL,
  },
}

export const LEXDAO_LOGO_ASCII = ``
